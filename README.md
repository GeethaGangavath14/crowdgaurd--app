# CrowdGuard
An AI-powered crowd monitoring and registration system.

## How to run
1. Clone this repository.
2. Install dependencies: `pip install -r requirements.txt` (after you create it).
3. Run the app: `streamlit run AI-18/streamlit_app.py`
